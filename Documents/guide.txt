guideline steps,
1. create a simple repo with c/c++

2. create a pipeline job in jenkins
pipeline syntax creation:
checkout scmGit(branches: [[name: '*/main']], extensions: [], userRemoteConfigs: [[credentialsId: 'Jenkins_token', url: 'https://github.com/Ajay107246/Demo.git']])

3. enable below option from pipeline job:
GitHub hook trigger for GITScm polling

4. look for remote/host creation e.g. ngrok creating web host for localhost:
ngrok http 8080 --host-header=\"localhost:8080\"
result:
1. create a simple repo with c/c++

2. create a pipeline job in jenkins
pipeline syntax creation:
checkout scmGit(branches: [[name: '*/main']], extensions: [], userRemoteConfigs: [[credentialsId: 'Jenkins_token', url: 'https://github.com/Ajay107246/Demo.git']])

3. enable below option from pipeline job:
GitHub hook trigger for GITScm polling

4. look for remote/host creation e.g. ngrok creating web host for localhost:

ps command to create index.html with value "hello world"

New-Item -Path . -Name "index.html" -ItemType File -Value "Hello, World!"


install choco from python

install ngrok from choco:
choco install ngrok 

[make sure you have account created on : https://dashboard.ngrok.com/]

then use this same directory with ngrok command:
ngrok 8080
or

ngrok http 8080 --host-header=\"localhost:8080\"
this will create random https url for you



add github-webhook/ as below,
use this url in your github repo -> setting -> webhook
e,g,
https://d11e-2a00-1e-e180-ec01-cc12-1422-651a-4f32.ngrok-free.app/github-webhook/

result:
https://d11e-2a00-1e-e180-ec01-cc12-1422-651a-4f32.ngrok-free.app -> this is random url generated by ngrok,
NOTE: this will change everytime, so need to update the webhook in github all time if you stop ngrok

or 
https://fecc-2a00-1e-e180-ec01-fda4-1af2-e4e-863c.ngrok-free.app




